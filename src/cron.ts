/**
 * Cron handler — called by the dispatch worker on a schedule.
 * Add your scheduled task logic here.
 */
export async function handleCron(_payload: unknown): Promise<void> {
  // No-op — implement your cron tasks here
}
